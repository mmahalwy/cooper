-- No-op: columns are already vector(768) from migration 003.
-- The embeddings provider now outputs 768 dimensions via outputDimensionality param.
-- This migration is kept for record-keeping only.
SELECT 1;
